#include<conio.h>
#include<stdio.h>

void main()
{
       char name[10];
       int i,j;
       clrscr();

       printf("Enter Name:");
       scanf("%s", name);

       for(i=0;name[i]!=NULL;i++);

       for(j=i-1;j>=0;j--)
       {
	printf("%c\n ", name[j]);
       }
getch();
}
